# pytest.ini is a setting file for pytest. When running pytest, it looks for this file first and reads the rules inside it before running any tests. 


[pytest]
markers =
    integration: requires downloaded PBS data in data/raw
# This means need real pbs files in `data/raw`